import React from 'react';
import {Text} from 'react-native';
import AppScreen from '../../components/App/Screen';

export const ProfilePreview = () => {
  return (
    // @ts-ignore
    <AppScreen>
      <Text>Profile Preview!</Text>
    </AppScreen>
  );
};

export default ProfilePreview;
